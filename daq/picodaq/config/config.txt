#CANALE A
y # attiva canale A

 d # DC coupling
 3 # range 3 = 50mV
 n # nessun offset
 y # imposto il trigger
  f # falling edge
  -3  # soglia trigger a mV

# CANALE B
n
 

# SETTING ACQUISITION PROPERTIES FOR RAPID BLOCK MODE
y # usa il default per #segmenti=1024 e #forme onda=100
n # non imposto la timebase automaticamente
 3 # Time per sample (ns)=(timebase-2)*8 1->2ns, 2-> 4ns, 3->8ns 4->16ns, 5->24ns, 6,  
 y # conferma
 n # usa i valori di default per nsamples/div e pre-post trigger
  50 # n -> Samples per DIV (tot num samples  = 10x num samples per div)
  100 # n -> PreTriggers (num samples saved before the trigger)
  400 # n -> PostTrigger (num samples saved after the trigger)
  y # conferma

# SETTING DURATION OF ACQUISITION
180 # acquisition time in seconds
30000 # max number of waveforms to acquire

