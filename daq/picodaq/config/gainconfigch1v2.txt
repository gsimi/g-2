#CANALE A
y # attiva canale A
 d # DC coupling
 4 # range 4 = 100mV
 n # nessun offset
 y # imposto il trigger
  f # falling edge
  -10  # soglia trigger a -10mV

# CANALE B
n

# SETTING ACQUISITION PROPERTIES FOR RAPID BLOCK MODE
y # usa il default per #segmenti=1024 e #forme onda=100
y # non imposto la timebase automaticamente
 5 # Time per sample (ns)=(timebase-2)*8 3->8ns 4->16ns, 5->24ns, 6,  
 y # conferma
 y # conferma (rispondendo n si pu`o impostare il pre/post trigger
  100 # n -> Samples per DIV
  200 # n -> PreTrigger
  500 # n -> PostTrigger
  y # conferma

# SETTING DURATION OF ACQUISITION
60 # acquisition time in seconds
500 # max number of waveforms to acquire

